# SELECT * FROM pg_catalog.pg_user
# SELECT * FROM generate_series(1, 100000)

# Insert some data using generate_series
DROP TABLE IF EXISTS example_table;
CREATE TABLE example_table (
	order_number VARCHAR(100) PRIMARY KEY,
	content VARCHAR ( 250 ) NOT NULL
);
INSERT INTO example_table(order_number, content)
SELECT i::text, 'this is just an example of data generated by generate_series function' FROM generate_series(1, 10000) AS t(i)

# Json config for postgresql dashboard on grafana (import this)
https://grafana.com/grafana/dashboards/9628-postgresql-database/
